// internal imports
import AdminWrapper from "@/layouts/admin-wrapper.tsx";

const AdminDashboard = () => {
    return (
        <AdminWrapper title="Admin dashboard">
            <p>Admin dashboard</p>
        </AdminWrapper>
    );
};

export default AdminDashboard;
